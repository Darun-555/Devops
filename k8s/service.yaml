apiVersion: v1
kind: Service
metadata:
  name: hospital-system-app-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: hospital-system-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000